version: 0.1
backend:
  phases:
    preBuild:
      commands:
        - npm install
        - npm run refresh
        - npm run test
    build:
      commands:
        - '# Execute Amplify CLI with the helper script'
        - amplifyPush --simple
